import HabitForm from '../widgets/HabitForm';
const AddTemplate: React.FC = () => {

    return (
        <div className="container mx-auto p-4">
            <HabitForm />
        </div>
    );
};

export default AddTemplate;
